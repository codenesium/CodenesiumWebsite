Do run the documentation locally you change to change
baseURL = "/documentation" to baseURL = "/"
in hte codumentation/config.toml file. 
Don't promote this change or the docs won't word on the site. 